name: "Locale addon 🌏"
on:
  push:
    branches:
      - "master"
    tags-ignore:
      - "v*"

jobs:
  locale:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - uses: DengSir/curse-locale-action@master
        with:
          token: ${{ secrets.CURSE_TOKEN }}

      - uses: GuillaumeFalourd/git-commit-push@v1.3
        with:
          commit_message: "chore: Update locale files"
          tags: false
